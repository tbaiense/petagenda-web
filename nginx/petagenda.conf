server {
    listen 80;
    root /usr/share/nginx/html;

    location / {
	try_files $uri $uri/ /index.html;
    }

    location ~ /api/v1/(.*)$ {
	proxy_pass http://petagenda-bd:3000/$1$is_args$args;
    }
}
